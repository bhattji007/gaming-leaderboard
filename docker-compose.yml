version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGO_URI=mongodb+srv://Bhattji007:Bhattji007@cluster0.ll2sgkg.mongodb.net/leaderboard
